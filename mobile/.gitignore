# Vite cache (not covered by root .gitignore)
.vite/

# Local Vite env files
*.local

# Generated by `tsc -b` for composite project (vite.config.ts → vite.config.{js,d.ts})
vite.config.js
vite.config.d.ts

# Capacitor — populated by DSHKI-47
# ios/ and android/ native projects are committed, but their build artifacts are not:
ios/App/build/
ios/App/Pods/
ios/DerivedData/
android/.gradle/
android/.idea/
android/local.properties
android/build/
android/app/build/
android/captures/
android/app/release/
